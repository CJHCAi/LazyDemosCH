# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AVPlayerViewControllerTest' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod 'Aspects'
  pod 'Masonry'
  # Pods for AVPlayerViewControllerTest

  target 'AVPlayerViewControllerTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AVPlayerViewControllerTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
